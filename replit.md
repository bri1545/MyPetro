# Мой Петропавловск - Партисипаторное бюджетирование

Полнофункциональная платформа партисипаторного бюджетирования для города Петропавловск.

## Технологический стек

- **Backend**: Go 1.23 с Chi router
- **Frontend**: HTMX + TailwindCSS
- **База данных**: PostgreSQL
- **ИИ-модерация**: Google Gemini 1.5 Flash
- **Карты**: Leaflet.js
- **Загрузка файлов**: Встроенная система хранения в /uploads

## Архитектура проекта

```
/cmd/server/main.go          # Точка входа приложения
/internal/
  handlers/                   # HTTP обработчики
  db/                        # Работа с базой данных
  models/                    # Модели данных
  auth/                      # Аутентификация
  ai/                        # ИИ-модерация (Gemini AI + встроенная логика)
  storage/                   # Загрузка файлов
  middleware/                # Middleware (требование авторизации)
/templates/                  # HTMX HTML шаблоны
/static/                     # Статические файлы
/uploads/                    # Загруженные фотографии проектов
```

## Основные функции

### 1. Система пользователей
- Регистрация с валидацией email и пароля (минимум 8 символов)
- Вход в систему
- Безопасные сессии через HTTP-only cookies
- Защита маршрутов с требованием авторизации

### 2. Подача идей проектов
- Форма с полями: название, описание (минимум 500 символов), категория, район, бюджет
- Выбор координат на интерактивной карте Leaflet
- Загрузка 1-3 фотографий (JPG/PNG, максимум 5MB)
- Фотографии сохраняются в `/uploads/{projectID}/`

### 3. ИИ-советник для модерации (Google Gemini AI)
**Новая логика модерации с ИИ-помощником** на основе **Gemini 1.5 Flash** в `/internal/ai/gemini.go`:

**Как работает модерация:**
1. Пользователь подаёт идею через форму → проект сразу попадает в статус `moderation`
2. ИИ автоматически анализирует проект и формирует список **плюсов** и **минусов**
3. Администратор видит идею вместе с анализом ИИ и принимает решение самостоятельно:
   - **Одобрить** → проект публикуется и идёт на голосование
   - **Отклонить** → проект не публикуется
   - **Редактировать** → админ может исправить название, описание, бюджет или категорию

**ИИ анализирует и выделяет:**
- **Плюсы:** общественная польза, улучшение городской среды, соответствие категориям, реалистичность бюджета
- **Минусы:** проблемы с бюджетом (вне диапазона 300k-2M ₸), короткое описание, отсутствие координат, личное использование, токсичность, несоответствие категориям

**ИИ не принимает решения — он лишь помогает админам**, делая модерацию быстрее и информативнее

### 4. Голосование с умными комментариями (Gemini AI)
- Обязательный комментарий при голосовании (минимум 200 символов)
- **Gemini AI валидация** комментариев в `/internal/ai/gemini.go`
- **ИИ отклоняет:** короткие фразы ("Нравится", "Круто"), токсичность, неконструктивные комментарии без обоснования
- **ИИ требует:** объяснение почему проект важен, польза для города и жителей, конкретное обоснование поддержки
- Один голос на проект на пользователя

### 5. Интерактивная карта Leaflet
- Отображение всех проектов с цветными маркерами по статусу:
  - 🟩 Серый - Голосование
  - 🟧 Оранжевый - В работе
  - 🟩 Зелёный - Завершён
- HTMX-загружаемые popup-карточки с информацией о проекте
- Пустое состояние: popup с призывом подать идею

### 6. Пустые состояния
- На странице проектов: "Пока нет идей. Будьте первым, кто предложит улучшение города!"
- На карте: Центральный popup с призывом к действию
- Кнопки для регистрации/подачи идеи

## База данных

### Таблица users
- id (SERIAL PRIMARY KEY)
- email (TEXT UNIQUE)
- password_hash (TEXT)
- created_at (TIMESTAMP)

### Таблица projects
- id (SERIAL PRIMARY KEY)
- title (TEXT)
- description (TEXT)
- category (TEXT)
- district (TEXT)
- budget (INT)
- lat, lng (FLOAT)
- images (JSONB)
- status (TEXT: moderation/voting/selected/in_progress/done/rejected)
- ai_analysis (TEXT) - JSON с плюсами и минусами от ИИ
- vote_start, vote_end (TIMESTAMP)
- user_id (INT FK)
- created_at (TIMESTAMP)

### Таблица votes
- id (SERIAL PRIMARY KEY)
- project_id (INT FK)
- user_id (INT FK)
- comment (TEXT)
- created_at (TIMESTAMP)
- UNIQUE(project_id, user_id)

## UI/UX

- Адаптивный дизайн с TailwindCSS
- Модальные окна для форм
- Панели голосования с валидацией
- Система загрузки фотографий
- Хедер с навигацией: Главная / Проекты / Карта / Вход / Регистрация
- Красивые пустые состояния
- HTMX для динамических обновлений без перезагрузки страницы

## Запуск

Сервер запускается автоматически через workflow:
```
go run cmd/server/main.go
```

Доступен на http://0.0.0.0:5000

## Переменные окружения

- `DATABASE_URL` - строка подключения к PostgreSQL (автоматически)
- `SESSION_SECRET` - секретный ключ для сессий (автоматически)
- `GEMINI_API_KEY` - API ключ Google Gemini для ИИ-модерации (обязательно)

## Категории проектов

1. Озеленение
2. Благоустройство
3. Скверы
4. Культура
5. Урбанистика

## Статусы проектов

- `moderation` - На модерации (после подачи идеи)
- `voting` - На голосовании (после одобрения администратором)
- `selected` - Победитель (набрал больше всего голосов)
- `in_progress` - В работе (реализация проекта)
- `done` - Завершён (проект реализован)
- `rejected` - Отклонён (на любом этапе)

## Последние изменения

### Ноябрь 24, 2025 - Профиль пользователя и адаптивная навигация
- **Профиль пользователя**: Добавлена страница профиля /profile с отображением ID, никнейма, email, роли и списка проектов пользователя
- **Никнеймы**: Добавлено поле nickname в модель User и БД, обязательное поле при регистрации
- **Адаптивная навигация**: Реализована полностью адаптивная навигация с Alpine.js
  - **Десктоп** (≥768px): Классическое горизонтальное меню с текстовыми ссылками
  - **Мобильные** (<768px): Бургер-меню (☰) с плавной анимацией выпадающего меню
- **Навигация**: Добавлена ссылка "Профиль" в меню для авторизованных пользователей
- **Исправления**: Безопасная обработка типов session user_id, обновлена команда создания администратора
- **Технические улучшения**: Alpine.js добавлен во все шаблоны, единое определение header для всех страниц

### Ноябрь 24, 2025 - Настройка Replit Environment
- Установлен Go 1.24 для работы с проектом
- Созданы необходимые директории: /static, /uploads
- Настроен .gitignore для Go проектов
- Загружены зависимости Go модулей
- Сконфигурирован workflow "Run Server" на порту 5000
- Настроен автоскейлинг deployment для production
- Установлены переменные окружения: SESSION_SECRET, GEMINI_API_KEY

### Ноябрь 24, 2025 - Основной функционал
- Создан полнофункциональный прототип платформы
- Реализована регистрация, аутентификация, подача идей
- **Интегрирована Google Gemini AI для умной модерации проектов и комментариев**
- Внедрена система голосования с ИИ-валидацией комментариев
- Создана интерактивная карта с Leaflet
- Реализованы пустые состояния UI
- ИИ анализирует качество идей и обоснованность голосов в реальном времени

### Ноябрь 24, 2025 - MVP улучшения
- **Система ролей**: добавлены роли citizen и admin
- **Админ-панель**: полноценная панель управления проектами
- **Новый жизненный цикл проектов**: moderation → voting → selected → in_progress → done / rejected
- **Комментарии для всех**: система комментариев доступна всем пользователям (не только при голосовании)
- **История статусов**: отслеживание всех изменений статуса проекта с комментариями администратора
- **Дедлайны голосования**: администратор устанавливает период голосования для каждого проекта
- **Таблицы БД**: comments, project_status_history, новые поля role, vote_start, vote_end

### Ноябрь 24, 2025 - Новая логика модерации с ИИ-советником
- **ИИ-советник вместо автоматической модерации**: ИИ анализирует проекты и формирует список плюсов и минусов
- **Администраторы принимают решения**: одобрить, отклонить или редактировать проект
- **Редактирование проектов**: админы могут исправлять название, описание, категорию и бюджет
- **Анализ сохраняется в БД**: новое поле `ai_analysis` хранит JSON с плюсами и минусами
- **Админ-панель обновлена**: отображение анализа ИИ для помощи в принятии решений
- **Проекты всегда создаются**: нет автоматического отклонения, все идеи попадают на модерацию

### Технические улучшения
- Настроен для работы в Replit environment с PostgreSQL
- Workflow и deployment сконфигурированы для production
- Middleware для проверки прав администратора
- Автоматические миграции схемы БД
- Новая функция AnalyzeIdeaWithGemini возвращает структурированный анализ
